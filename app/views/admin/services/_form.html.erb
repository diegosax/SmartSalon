<%= form_for([:admin, @service]) do |f| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <b>Nome:</b> <%= f.text_field :name %>
  </div>
  <div class="field">
    <b>Duração:</b> <%= f.number_field :duration %>
  </div>
  <div class="field">
    <b>Preço:</b> <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
